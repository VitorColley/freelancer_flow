<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "Freelancer Flow", root_path, class: "navbar-brand fw-semibold" %>

    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
        <li class="nav-item">
          <%= link_to "Projects", projects_path, class: "nav-link" %>
        </li>

        <% if current_user %>
          <li class="nav-item text-white small me-2 d-none d-lg-block">
            Signed in as <strong><%= current_user.name.presence || current_user.email_address %></strong>
          </li>

          <li class="nav-item">
            <%= link_to "Logout",
                        session_path,
                        data: { turbo_method: :delete },
                        class: "btn btn-outline-light btn-sm" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login",
                        new_session_path,
                        class: "btn btn-primary btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
